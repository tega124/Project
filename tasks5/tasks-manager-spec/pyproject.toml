[project]
name = "taskmgr"
version = "0.1.0"
description = "Spec-kit generated task manager for CSC299"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = ["."]
