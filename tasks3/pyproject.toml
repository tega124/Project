[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tasks3"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
tasks3 = "tasks3:main"
