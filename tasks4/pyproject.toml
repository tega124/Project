[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tasks4"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["openai>=1.50.0"]

[project.scripts]
tasks4 = "tasks4:main"
